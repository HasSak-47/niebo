header stdio::printf;

type testType = i32;

impl testType {
    const fn next(&self) -> testType {
        return self + 1;
    }
}

fn main() -> testType {
    let mut i : testType = 0;
    while i < 10{
        printf("%d\n", i);
        i.next();
    }
}
