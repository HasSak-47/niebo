import super::traits::*;

extend i32 as Copy {
    func copy(&self) -> i32 {
        return @copy(i32);
    }
}

extend i32 as Add<i32, i32> {
    func add(self, other: i32) -> i32 {
        return @add_i32_i32(self, other);
    }
}
